name: CI

on:
  pull_request:
  push:
    branches: [main, master, "**"]

jobs:
  checks:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v20

      - name: Enable magic Nix cache
        uses: DeterminateSystems/magic-nix-cache-action@v6

      - name: Formatting
        run: nix fmt -- --fail-on-change

      - name: Flake checks
        run: nix flake check

      - name: Dev services app evaluates
        run: nix run .#devservices -- --help

  # Optional: FlakeHub publish on tags (commented scaffold)
  # publish:
  #   if: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && secrets.FLAKEHUB_TOKEN != '' }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: DeterminateSystems/nix-installer-action@v20
  #     - name: Publish to FlakeHub
  #       run: |
  #         echo "TODO: Add flakehub publish command or action here."
  #         # Keep disabled until token and exact action are confirmed.
